/**
 * Re-export of the canonical latest-sensor-snapshot picker so vitest can
 * import it from `src/**`. Canonical implementation lives at
 * `supabase/functions/ai-coach/latestSensorSnapshot.ts`.
 */
export * from "../../supabase/functions/ai-coach/latestSensorSnapshot";
