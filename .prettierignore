bun.lockb
package-lock.json
yarn.lock
pnpm-lock.yaml
dist
build
node_modules
.husky
src/integrations/supabase/types.ts
supabase/functions/_shared/lib/integrations/supabase/types.ts
# Generated by TanStack Router — its header instructs excluding it from
# linters/formatters; reformatting breaks the single-quote route-id pins in
# src/test/authenticated-detail-route-unnesting.test.ts.
src/routeTree.gen.ts
docs/archive/legacy/verdant-master-prompt-legacy.md
artifacts/seo/seo-readiness-status.json
# The Sentinel governance mirror — GEMINI.md embeds AGENTS.md verbatim inside its
# SENTINEL-CORE block, and scripts/check-sentinel-version-parity.mjs requires the two to
# stay byte-equivalent. Prettier formats the same bytes differently in each file, so
# formatting either one desynchronises the mirror and fails the MIRROR check. Regenerate
# with scripts/sync-sentinel-mirror.mjs, never by hand and never by a formatter.
AGENTS.md
GEMINI.md
