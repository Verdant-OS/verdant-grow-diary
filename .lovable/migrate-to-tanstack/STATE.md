# TanStack Start migration — in-progress state

Resume with: "continue the migration". Undo with: revert this turn from chat history.

## Completed

**Preflight** — eligibility passed (Classic `vite_*` stack); `bun run build` green on the
pre-migration baseline.

**Step 0 — scan.** 114 flat routes in `src/App.tsx`; providers
`RootErrorBoundary > QueryClientProvider > TooltipProvider > BrowserRouter > AuthProvider >
GrowsProvider`; 36 edge functions; bespoke prerender/OG/JSON-LD Vite pipeline; loose
TypeScript across ~1,700 files.

**Step 1 — scaffolding.** Written: `tsconfig.json`, `vite.config.ts`, `bunfig.toml`,
`eslint.config.js`, `components.json`, `src/router.tsx`, `src/server.ts`, `src/start.ts`,
`src/lib/error-page.ts`, `src/lib/error-capture.ts`, `src/lib/lovable-error-reporting.ts`,
`src/styles.css`.

Theme ported in full from `src/index.css` + `tailwind.config.ts` to Tailwind v4 — every HSL
token, the `.customer-mode` alt theme, gradients/shadows, `font-display`/`font-mono`, the
container, all four keyframes/animations, component + utility layers, print-report rules.
Recorded in `.lovable/migrate-to-tanstack/theme-port.json`.

v4 sweep: `bg-[--x]` → `bg-[var(--x)]` in `src/components/ui/sidebar.tsx` and
`src/components/ui/chart.tsx`; `outline-none` → `outline-hidden` across 104 files.

**Step 3 — package.json merged.** TanStack Start 1.168 / Router 1.170 / React 19 / Tailwind 4
/ vite 8 / nitro; `react-router-dom` dropped. All 274 user scripts plus `lint-staged` and
`overrides` carried through.

**Step 4 — SPA entry points removed.** Deleted `index.html`, `src/main.tsx`, `src/App.tsx`,
`src/App.css`, `src/index.css`, `src/vite-env.d.ts`, `tsconfig.app.json`,
`tsconfig.node.json`, `tailwind.config.ts`, `postcss.config.js`, and two stale `.bak` configs.
`tsconfig.app.json` references repointed to `tsconfig.json` in
`tsconfig.irrigation-harness.json`, 4 CI workflows, and 1 test.
Backups: `/tmp/migrate-to-tanstack/{App.tsx,index.html,main.tsx,index.css}.bak`.

**Step 5 — routes generated.** `src/routes/__root.tsx` (providers, full `index.html` head
port: GA4 with `send_page_view:false`, Organization+WebSite JSON-LD, OG/Twitter, canonical,
favicon/manifest, fonts; plus `errorComponent`). Layout routes `_app.tsx` (AppShell) and
`_app/_operator.tsx` (RequireOperatorRole). 138 route files emitted from the App.tsx
manifest — 53 public, 50 AppShell, 35 operator, matching the wrapper inventory exactly.
`LegacyStrainSlugRedirect` extracted to its own component.

**Step 6 — router compat shim.** `src/lib/react-router-compat.tsx` implements `Link`,
`NavLink`, `Navigate`, `Outlet`, `useNavigate` (incl. `navigate(-1)`), `useLocation`,
`useParams`, `useSearchParams`, `useInRouterContext` over TanStack with the react-router
calling convention. All 651 `react-router-dom` importers repointed to it.

## Not started / not verified

- **No build, typecheck, or install has been run since Step 1.** `routeTree.gen.ts` does not
  exist yet — it is generated by the first `bun run build`.
- Steps 7–8 (Supabase SSR `sessionStorage` guard, edge-function classification) not started.
  `src/integrations/supabase/client.ts` references `sessionStorage` at module scope and
  **will throw during SSR** until guarded — this is the first predictable Step 9 failure.
- Steps 9–10 not started.

## Deferred / needs a decision on resume

1. **Bespoke `vite.config.ts` prerender pipeline was overwritten** by the Step 1 template
   write (Resvg OG cards, `viteManualChunks`, `buildStaticSocialRouteHtml`, JSON-LD
   injection). Helper modules under `scripts/` and `src/lib/` are untouched; only the ~80-line
   wiring file is gone, recoverable from local git. Needs re-expressing as TanStack `head()`
   + prerender config, and the postbuild validators (`check:jsonld`, `check:og-images`,
   `check:canonical-*`, `check:sitemap-robots`) repointed at the new `dist/` shape.
2. **`vitest.config.ts` intentionally kept** against the skill's delete instruction — 274 test
   scripts and ~30 CI workflows bind to it. Still needs repointing at `src/styles.css`.
3. **TypeScript strict flip** — `strict`, `strictNullChecks`, `noUncheckedIndexedAccess`,
   `exactOptionalPropertyTypes` now on over a codebase that ran with `strictNullChecks:false`
   and `noImplicitAny:false`. Largest remaining item; Step 9 cannot go green until absorbed.
4. **Legacy router primitives** (`BrowserRouter`, `MemoryRouter`, `Routes`, `Route`) are
   compile-only passthrough shims in the compat layer. Unit tests that wrap components in
   `<MemoryRouter>` will compile but no longer exercise routing — those tests need rewriting
   against the TanStack router.
5. **Tailwind v4 scale renames unapplied** — `shadow-sm`→`shadow-xs`, bare `shadow`→
   `shadow-sm`, `rounded`/`blur` equivalents, bare `ring`→`ring-3`. Bulk rewriting was unsafe
   because the same words appear in grower-facing copy constants. 1px visual drift, not a break.
