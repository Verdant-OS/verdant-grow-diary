# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
desktop.ini
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.one-tent-loop-smoke-report.json
tmp/

# Verdant EcoWitt Windows Testbench (local-only)
tools/ecowitt-testbench/.env
tools/ecowitt-testbench/.venv/
tools/ecowitt-testbench/ecowitt_raw_log.jsonl
tools/ecowitt-testbench/__pycache__/
.artifacts/

# MCP local RLS harness debug output (uploaded as CI artifacts on failure,
# sanitized, never committed)
artifacts/

# Verdant SEO monitoring — local OAuth token cache
.seo/

# Playwright transient output (uploaded as CI artifacts, never committed)
test-results/
playwright-report/
